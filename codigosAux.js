//Criar tabelas
//db.run("CREATE TABLE problems (id number, problem string, description string, correctWords string, randomWords string, xp number, level string, lock string, completed string)");
// db.run("CREATE TABLE score (id_usuario number, id number ,totalXp number)");
// db.run("CREATE TABLE desafio (id number, id_problem number)");

//inserir dados
//db.run("INSERT INTO problems (id, problem, description, correctWords, randomWords, xp, level, lock, completed) VALUES (1, '??(3 === 3) {console.log(´acertou´)}', 'O numero 3 é igual a 3?',  'if, for', 'for, while, if, break, i++, const, forEach', 16, 'easy' , 'true', 'true')");
// db.run("INSERT INTO score (id_usuario, id, totalXp) VALUES (1, 1, 0)");
// db.run("INSERT INTO desafio (id, id_problem) VALUES (1, 1)");